apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: overseerr-ingress-route
  namespace: networking
  annotations: 
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`overseerr.unrelated.family`)
      kind: Rule
      services:
        - name: overseerr-docker
          port: 443
  tls:
    secretName: unrelated-family-tls
---
apiVersion: v1
kind: Service
metadata:
  name: overseerr-docker
  namespace: networking
spec:
  type: ExternalName
  externalName: "artificer.local.unrelated.family"
  ports:
    - name: https
      port: 443
      protocol: TCP
      targetPort: 443